{
  "name": "vitalparse",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "start:functions": "npm run start --workspace=apps/functions",
    "start:api": "npm run start --workspace=apps/api",
    "dev:api": "npm run dev --workspace=apps/api"
  }
}
